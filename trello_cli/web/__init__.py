"""Optional web app for TrelloCLI (FastAPI + vanilla-JS drag-drop kanban).

Importing this subpackage requires the `[web]` extra
(`pip install trello-cli[web]`). The core CLI stays httpx-only. See DESIGN.md.
"""
